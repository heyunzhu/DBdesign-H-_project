# 图书借阅管理系统阶段演示说明

## 一、阶段目标

本阶段目标是完成系统核心功能开发与阶段整合，使前端、后端和 MySQL 数据库能够形成基本联动。系统需要支持图书借阅业务中的数据新增、查询、修改和删除操作，并能够展示若干关键 SQL 查询的实际效果。

当前项目已经完成数据库建表、初始数据导入、后端接口开发、前端页面联动、登录鉴权、角色权限控制和读者借书助手 Agent 的基础集成。

## 二、项目结构

```text
Library
├─ SQL                         数据库脚本
│  ├─ schema.sql               建表脚本
│  ├─ seed.sql                 初始测试数据
│  ├─ indexes.sql              索引设计
│  ├─ queries.sql              关键 SQL 查询
│  ├─ explain.sql              查询执行计划验证
│  └─ auth_upgrade.sql         账号密码字段升级脚本
├─ backend                     Spring Boot + MyBatis 后端
│  ├─ src/main/java            控制器、服务、实体、Mapper、鉴权、Agent
│  └─ src/main/resources       application.yml 与 MyBatis XML
└─ frontend                    Vue3 + Vite 前端
   └─ src                      页面、接口请求与样式
```

## 三、运行环境

- MySQL 8.0，端口使用 `3307`，数据库名为 `library`
- JDK 17 及以上
- Maven
- Node.js 与 npm
- 后端端口：`8081`
- 前端开发端口：通常为 `5173`

如果使用读者借书助手 Agent，需要额外配置 DeepSeek API Key：

```powershell
$env:DEEPSEEK_API_KEY="你的 DeepSeek API Key"
```

## 四、数据库初始化

在 MySQL Workbench 或命令行中依次执行：

```sql
SOURCE E:/数据库Pj/Library/SQL/schema.sql;
SOURCE E:/数据库Pj/Library/SQL/seed.sql;
SOURCE E:/数据库Pj/Library/SQL/indexes.sql;
SOURCE E:/数据库Pj/Library/SQL/auth_upgrade.sql;
```

如果不是使用 `SOURCE`，也可以在 Workbench 中打开对应 SQL 文件，按顺序执行。

数据库脚本实现了以下内容：

- 创建用户、角色、权限、作者、图书分类、图书、借阅记录等核心表。
- 设置主键、外键、唯一约束和状态检查约束。
- 插入初始角色、权限、用户、作者、分类、图书和借阅记录。
- 为常用查询字段建立必要索引。
- 为用户登录补充 `password_hash` 字段。

## 五、启动方式

### 1. 启动 MySQL

确认 MySQL80 服务已经启动，并且 `library` 数据库可以连接。

如果在普通命令行执行 `net start MySQL80` 出现“拒绝访问”，需要使用管理员身份打开 PowerShell 或命令提示符后再执行。

### 2. 启动后端

```powershell
cd E:\数据库Pj\Library\backend
mvn.cmd spring-boot:run
```

后端启动成功后，控制台应出现类似信息：

```text
Tomcat started on port 8081
Started LibraryBackendApplication
```

可以访问健康检查接口验证：

```text
http://localhost:8081/api/health
```

正常返回：

```json
{"code":200,"message":"success","data":{"status":"ok"}}
```

### 3. 启动前端

```powershell
cd E:\数据库Pj\Library\frontend
npm run dev
```

启动后访问终端输出的地址，通常是：

```text
http://localhost:5173
```

## 六、测试账号

| 角色 | 账号 | 密码 | 说明 |
| --- | --- | --- | --- |
| 普通读者 | `20240001` | `123456` | 可查询图书、借书、还书、查看自己的借阅记录、使用读者借书助手 |
| 管理员 | `A2024001` | `123456` | 可进行图书、用户、借阅和统计管理 |

## 七、核心功能演示

### 1. 登录与权限控制

演示步骤：

1. 打开前端页面。
2. 使用普通读者账号登录。
3. 观察普通读者只能看到图书查询、我的借阅、读者借书助手等功能。
4. 退出登录后，使用管理员账号登录。
5. 观察管理员可以进入图书管理、用户管理、借阅管理和统计分析等功能。

预期效果：

- 未登录用户无法访问受保护接口。
- 普通读者不能看到其他读者的借阅信息。
- 普通读者不能进入用户管理和图书管理操作。
- 管理员可以执行管理类操作。

### 2. 图书查询

演示步骤：

1. 登录系统。
2. 进入图书页面。
3. 使用关键词搜索，例如“数据库”“算法”“数据结构”。
4. 查看图书名称、作者、分类、出版社、出版日期和借阅状态。

对应数据操作：

- 查询 `book` 表。
- 联查 `author` 表和 `book_type` 表。
- 根据书名、作者、分类和状态筛选图书。

### 3. 图书管理

演示步骤：

1. 使用管理员账号登录。
2. 新增一本测试图书。
3. 修改该图书的出版社、分类或状态。
4. 删除测试图书。

对应数据操作：

- 新增：向 `book` 表插入记录。
- 查询：读取图书列表和图书详情。
- 修改：更新 `book` 表中的图书信息。
- 删除：删除或停用指定图书记录。

### 4. 借阅与归还

演示步骤：

1. 使用读者账号登录。
2. 在图书页面选择一本可借图书进行借阅。
3. 进入借阅记录页面，查看新产生的借阅记录。
4. 对该记录执行归还。
5. 再次查看借阅记录和图书状态变化。

对应数据操作：

- 借书时向 `borrow_record` 表新增一条借阅记录。
- 借书时将 `book.book_status` 修改为借出状态。
- 还书时更新 `borrow_record.actual_return_time` 和 `borrow_record.borrow_status`。
- 还书时将 `book.book_status` 恢复为可借状态。

### 5. 用户管理

演示步骤：

1. 使用管理员账号登录。
2. 进入用户管理页面。
3. 新增用户，设置账号、姓名、角色、状态和密码。
4. 修改用户信息。
5. 禁用或启用用户账号。

对应数据操作：

- 新增、查询、修改 `sys_user` 表。
- 通过 `role_id` 关联 `sys_role` 表。
- 通过 `account_status` 控制账号是否可用。

### 6. 统计分析

演示步骤：

1. 使用管理员账号登录。
2. 进入统计分析页面。
3. 查看分类图书数量、逾期记录、借阅排行等统计结果。

对应数据操作：

- 对 `book`、`book_type` 和 `borrow_record` 进行聚合查询。
- 使用 `COUNT`、`GROUP BY`、`ORDER BY` 展示统计结果。

### 7. 读者借书助手 Agent

演示步骤：

1. 使用普通读者账号登录。
2. 进入读者借书助手页面。
3. 输入“我想找一本数据库相关的书”。
4. 系统返回可借图书推荐。
5. 输入“帮我借第一本”或“帮我借《数据库系统概论》”。
6. 查看借阅记录是否新增。
7. 输入“我现在借了什么书”。
8. 输入“帮我归还《书名》”。

预期效果：

- Agent 可以根据自然语言查询图书。
- Agent 可以将“第一本”“就这本”“刚才那本”等表达转换为实际图书。
- Agent 可以调用后端工具完成借阅和归还。
- Agent 只能由普通读者使用，管理员无此服务。

## 八、关键 SQL 展示

以下 SQL 可在 MySQL Workbench 中运行，用于展示数据库查询效果。

### 1. 查询所有可借图书

```sql
SELECT
    book_id,
    isbn,
    book_name,
    publisher,
    publish_date
FROM book
WHERE book_status = 0;
```

展示意义：

- 验证图书状态字段是否可用于筛选可借图书。
- 对应前端图书列表和读者选书场景。

### 2. 查询图书详细信息

```sql
SELECT
    b.book_id,
    b.book_name,
    b.isbn,
    a.author_name,
    t.type_name,
    b.publisher,
    b.publish_date,
    CASE b.book_status
        WHEN 0 THEN '在馆可借'
        WHEN 1 THEN '借出中'
        WHEN 2 THEN '停用/下架'
    END AS book_status_text
FROM book b
JOIN author a ON b.author_id = a.author_id
JOIN book_type t ON b.type_id = t.type_id;
```

展示意义：

- 验证多表关联查询。
- 对应图书详情展示。

### 3. 查询用户借阅历史

```sql
SELECT
    br.borrow_id,
    u.user_name,
    u.user_no,
    b.book_name,
    br.borrow_time,
    br.due_return_time,
    br.actual_return_time,
    CASE br.borrow_status
        WHEN 0 THEN '未归还'
        WHEN 1 THEN '已归还'
        WHEN 2 THEN '逾期'
    END AS borrow_status_text
FROM borrow_record br
JOIN sys_user u ON br.user_id = u.user_id
JOIN book b ON br.book_id = b.book_id
ORDER BY br.borrow_time DESC;
```

展示意义：

- 验证借阅记录与用户、图书之间的关联。
- 对应管理员查看借阅记录和读者查看个人借阅记录。

### 4. 查询当前逾期未还记录

```sql
SELECT
    br.borrow_id,
    u.user_name,
    u.user_no,
    b.book_name,
    br.borrow_time,
    br.due_return_time,
    DATEDIFF(CURRENT_DATE, br.due_return_time) AS overdue_days
FROM borrow_record br
JOIN sys_user u ON br.user_id = u.user_id
JOIN book b ON br.book_id = b.book_id
WHERE br.borrow_status IN (0, 2)
  AND br.actual_return_time IS NULL
  AND br.due_return_time < CURRENT_TIMESTAMP
ORDER BY overdue_days DESC;
```

展示意义：

- 验证逾期判断逻辑。
- 对应统计分析中的逾期记录。

### 5. 统计每个分类下的图书数量

```sql
SELECT
    t.type_name,
    COUNT(b.book_id) AS book_count
FROM book_type t
LEFT JOIN book b ON t.type_id = b.type_id
GROUP BY t.type_id, t.type_name
ORDER BY book_count DESC;
```

展示意义：

- 验证分组统计查询。
- 对应统计分析中的分类图书数量。

### 6. 统计借阅数量最多的图书分类

```sql
SELECT
    t.type_name,
    COUNT(br.borrow_id) AS borrow_count
FROM borrow_record br
JOIN book b ON br.book_id = b.book_id
JOIN book_type t ON b.type_id = t.type_id
GROUP BY t.type_id, t.type_name
ORDER BY borrow_count DESC;
```

展示意义：

- 验证借阅记录聚合分析。
- 对应系统后续推荐和馆藏优化场景。

## 九、索引与性能验证

项目已经在 `SQL/indexes.sql` 中设计了针对常用查询的索引，主要包括：

- `book(book_status, book_id)`：支持按图书状态筛选和分页查询。
- `book(type_id, book_status)`：支持按分类和状态筛选图书。
- `sys_user(role_id, account_status, user_id)`：支持按角色和账号状态查询用户。
- `borrow_record(user_id, borrow_time, borrow_id)`：支持查询指定用户借阅历史。
- `borrow_record(user_id, borrow_status, borrow_time, borrow_id)`：支持查询指定用户当前借阅记录。
- `borrow_record(borrow_status, due_return_time)`：支持逾期未还记录查询。

可以执行 `SQL/explain.sql` 中的 `EXPLAIN` 语句观察查询执行计划，重点查看：

- `key` 是否命中预期索引。
- `rows` 扫描行数是否减少。
- `Extra` 中是否出现明显的全表扫描风险。

## 十、阶段完成情况

| 验收要求 | 当前完成情况 |
| --- | --- |
| 前端、后端、数据库形成基本联动 | 已完成 |
| 支持核心业务场景下的数据新增 | 已完成，支持新增图书、用户、借阅记录 |
| 支持核心业务场景下的数据查询 | 已完成，支持图书、用户、借阅、统计查询 |
| 支持核心业务场景下的数据修改 | 已完成，支持修改图书、用户、借阅归还状态 |
| 支持核心业务场景下的数据删除 | 已完成，支持图书删除或停用 |
| 展示关键 SQL 查询实际效果 | 已完成，可通过 `SQL/queries.sql` 和本说明中的 SQL 展示 |
| 权限控制 | 已完成，区分普通读者、图书管理员和系统管理员 |
| 读者借书助手 Agent | 已完成基础功能，支持查书、借书、还书和查询个人借阅 |

## 十一、演示建议顺序

建议正式演示时按以下顺序进行：

1. 展示项目结构和数据库脚本。
2. 在 MySQL 中执行关键 SQL 查询，说明表结构、约束和索引。
3. 启动后端，访问 `/api/health` 验证后端正常。
4. 启动前端，使用读者账号登录。
5. 演示读者查书、借书、查看个人借阅、归还。
6. 演示读者借书助手 Agent。
7. 使用管理员账号登录。
8. 演示图书管理、用户管理、借阅管理和统计分析。
9. 回到 MySQL 查看借阅记录和图书状态变化，说明前后端操作已经真实写入数据库。

## 十二、后续可完善内容

- 将数据库密码改为环境变量配置，避免本地密码写入配置文件。
- 为 Agent 增加更稳定的多轮上下文持久化，避免后端重启后丢失会话状态。
- 为登录密码增加加盐哈希，提高安全性。
- 增加后端单元测试和接口测试。
- 增加演示截图或录屏材料，方便最终答辩展示。
