# 第四阶段阶段报告

邢涵博 梁熙哲

## Github 链接    
https://github.com/heyunzhu/DBdesign-H-_project.git  

## 数据库基本信息

本项目使用 MySQL 8.0 作为关系型数据库，数据库名称为 `library`。

数据库相关文件位于：

```text
Library/SQL/
```

主要文件包括：

| 文件名 | 说明 |
| --- | --- |
| `schema.sql` | 建库建表 SQL，包含表结构和主要约束 |
| `seed.sql` | 初始测试数据 |
| `indexes.sql` | 索引设计 SQL |
| `queries.sql` | 基础查询、多表查询、聚合查询和业务查询 |
| `test.sql` | 约束测试 SQL |
| `explain.sql` | 查询计划验证 SQL |
| `archive_design.sql` | 借阅历史归档扩展设计 |
| `数据库说明.md` | 数据库设计与实现说明文档 |

## 表结构实现

本阶段已完成 8 张核心表的创建：

| 表名 | 作用 |
| --- | --- |
| `sys_role` | 保存系统角色，如普通读者、图书管理员、系统管理员 |
| `sys_permission` | 保存系统权限项 |
| `sys_user` | 保存用户账号信息 |
| `sys_role_permission` | 保存角色与权限之间的多对多关系 |
| `author` | 保存作者信息 |
| `book_type` | 保存图书分类信息 |
| `book` | 保存图书馆馆藏图书信息 |
| `borrow_record` | 保存图书借阅和归还记录 |

这些表能够覆盖系统中的用户管理、角色权限、图书管理、借阅归还、借阅记录查询和统计查询等核心业务场景。

## 主要约束实现方案

数据库中已实现以下约束：

1. 主键约束  
每张表均设置主键，例如 `user_id`、`book_id`、`borrow_id`，用于唯一标识记录。

2. 外键约束  
通过外键维护表之间的引用完整性，例如：

```text
sys_user.role_id -> sys_role.role_id
book.author_id -> author.author_id
book.type_id -> book_type.type_id
borrow_record.user_id -> sys_user.user_id
borrow_record.book_id -> book.book_id
```

外键删除策略采用 `RESTRICT`，避免删除已被引用的数据而破坏历史记录。

3. 唯一约束  
对关键业务字段设置唯一约束，例如：

```text
sys_user.user_no
sys_role.role_name
sys_permission.permission_code
book.isbn
book_type.type_name
sys_role_permission(role_id, permission_id)
```

4. 非空约束  
对核心业务字段设置 `NOT NULL`，例如用户名、角色名、图书名称、借阅时间、应归还时间等。

5. CHECK 约束  
使用 MySQL 8.0 的 `CHECK` 约束限制状态字段取值和借阅时间逻辑，例如：

```text
account_status 只能为 0 或 1
book_status 只能为 0、1、2
borrow_status 只能为 0、1、2
due_return_time 必须晚于 borrow_time
归还状态与 actual_return_time 必须保持一致
```

6. 默认值约束  
注册时间、创建时间、借阅时间等字段使用当前时间作为默认值，账号状态、图书状态、借阅状态也设置了默认初始值。

## 索引设计

本阶段新增 `indexes.sql`，为高频查询建立必要索引。主要索引包括：

| 索引名 | 作用 |
| --- | --- |
| `idx_sys_user_role_status` | 支持用户按角色和账号状态筛选 |
| `idx_book_status_id` | 支持图书按状态筛选和分页查询 |
| `idx_book_type_status` | 支持图书分类统计和分类筛选 |
| `idx_borrow_status_due_time` | 支持逾期未还记录查询 |
| `idx_borrow_user_time` | 支持用户借阅历史查询 |
| `idx_borrow_user_status_time` | 支持用户按状态筛选借阅记录 |

同时提供 `explain.sql`，用于通过 `EXPLAIN` 查看查询计划，验证索引是否能够服务典型业务查询。

## 初始测试数据

本阶段已完成 `seed.sql`，包含系统演示和测试所需的初始数据：

```text
3 个角色
多个权限项
角色权限关联数据
多个用户
多个作者
多个图书分类
20 本图书
多条借阅记录
```

这些数据能够支持图书查询、用户查询、借阅历史查询、逾期查询和分类统计等功能演示。

## 查询与测试验证

`queries.sql` 中已包含课程要求的查询类型：

```text
基础查询
多表连接查询
聚合查询
业务统计查询
```

`test.sql` 中包含约束测试，例如：

```text
重复学号插入测试
非法状态值测试
非法外键引用测试
重复角色权限分配测试
删除被引用数据测试
不合理借阅时间测试
```

执行这些测试时，非法数据应被数据库约束拦截，从而证明数据库层约束设计有效。

