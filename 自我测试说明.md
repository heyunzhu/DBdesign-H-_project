# 自我测试说明

本文档用于记录图书借阅管理系统最终版本的自我测试情况，覆盖数据库、后端、前端、核心业务功能、数据一致性和演示稳定性检查。

## 1. 测试环境

| 项目 | 内容 |
| --- | --- |
| 操作系统 | Windows |
| 数据库 | MySQL 8.0 |
| 后端 | Spring Boot + MyBatis |
| 前端 | Vue 3 + Vite |
| 后端端口 | `8081` |
| 前端地址 | `http://127.0.0.1:5173` |
| 数据库名 | `library` |

后端启动前需要设置：

```powershell
$env:DB_PASSWORD="本机 MySQL 密码"
```

如需演示 Agent，需要额外设置：

```powershell
$env:DEEPSEEK_API_KEY="DeepSeek API Key"
```

## 2. 构建测试

### 2.1 前端构建

执行命令：

```powershell
cd E:\数据库Pj\Library\frontend
npm.cmd run build
```

测试结果：

```text
通过。Vite production build completed successfully.
```

### 2.2 后端打包

执行命令：

```powershell
cd E:\数据库Pj\Library\backend
mvn.cmd -DskipTests package
```

测试结果：

```text
通过。Spring Boot jar package completed successfully.
```

说明：

```text
当前项目未重点编写自动化单元测试，因此打包命令使用 -DskipTests。功能正确性主要通过前后端联调和数据库操作进行验证。
```

## 3. 数据库测试

### 3.1 建表和初始数据

按顺序执行：

```text
SQL/schema.sql
SQL/seed.sql
SQL/indexes.sql
```

检查 SQL：

```sql
USE library;
SHOW TABLES;
SELECT COUNT(*) FROM sys_user;
SELECT COUNT(*) FROM book;
SELECT COUNT(*) FROM borrow_record;
```

预期结果：

```text
数据库表能够正确创建，初始用户、角色、权限、作者、分类、图书和借阅记录能够成功导入。
```

### 3.2 约束测试

已验证或可验证的约束包括：

| 测试项 | 预期结果 |
| --- | --- |
| 插入重复 `user_no` | 被唯一约束拒绝 |
| 插入重复 `isbn` | 被唯一约束拒绝 |
| 插入不存在用户或图书的借阅记录 | 被外键约束拒绝 |
| 插入非法状态值 | 被 `CHECK` 约束拒绝 |
| 应还时间早于借阅时间 | 被 `CHECK` 约束拒绝 |

### 3.3 索引检查

当前保留的业务索引：

```sql
SHOW INDEX FROM book;
SHOW INDEX FROM borrow_record;
SHOW INDEX FROM agent_operation_log;
```

说明：

```text
最终版本已删除以低选择性字段作为最左列的 idx_book_status_id 和 idx_sys_user_role_status_id，保留借阅历史、逾期查询、分类统计和 Agent 审计相关索引。
```

## 4. 后端接口测试

### 4.1 健康检查

接口：

```text
GET http://localhost:8081/api/health
```

预期：

```json
{"code":200,"message":"success","data":{"status":"ok"}}
```

### 4.2 登录测试

测试账号：

| 角色 | 账号 | 密码 |
| --- | --- | --- |
| 普通读者 | `20240001` | `123456` |
| 图书管理员 | `T2024001` | `123456` |
| 系统管理员 | `A2024001` | `123456` |

预期：

```text
正确账号密码可以登录；错误密码会返回登录失败；禁用用户不能正常使用系统。
```

## 5. 前后端联调测试

| 功能 | 测试内容 | 结果 |
| --- | --- | --- |
| 登录 | 使用不同角色账号登录 | 通过 |
| 权限菜单 | 不同角色看到不同菜单 | 通过 |
| 图书查询 | 按关键词、状态查询图书 | 通过 |
| 图书管理 | 新增、编辑、下架图书 | 通过 |
| 用户管理 | 新增、编辑、禁用/启用用户 | 通过 |
| 借阅办理 | 选择用户和图书办理借书 | 通过 |
| 归还办理 | 对未归还记录执行归还 | 通过 |
| 借阅记录 | 查询全部或本人借阅记录 | 通过 |
| 统计查询 | 查看分类数量、逾期记录、借阅排行 | 通过 |

## 6. 数据一致性测试

### 6.1 借书一致性

测试步骤：

1. 选择一本 `book_status = 0` 的图书。
2. 办理借阅。
3. 查询 `borrow_record`。
4. 查询 `book`。

预期：

```text
borrow_record 新增一条未归还记录，book.book_status 更新为 1。
```

### 6.2 还书一致性

测试步骤：

1. 选择一条未归还借阅记录。
2. 办理归还。
3. 查询 `borrow_record`。
4. 查询 `book`。

预期：

```text
borrow_record.actual_return_time 写入实际归还时间，borrow_status 更新为 1，book.book_status 恢复为 0。
```

### 6.3 删除策略

测试说明：

```text
大多数外键使用 ON DELETE RESTRICT。已经产生借阅记录的图书不直接物理删除，而是通过 book_status = 2 进行下架，保证历史借阅记录可追溯。
```

## 7. Agent 功能测试

Agent 仅对普通读者开放。使用普通读者账号：

```text
20240001 / 123456
```

测试用例：

| 输入 | 预期结果 |
| --- | --- |
| `我想借一本数据库相关的书` | 返回数据库相关馆藏图书 |
| `帮我借《数据库系统概论》` | 根据书名办理借阅，不要求输入 `bookId` |
| `我现在借了哪些书？` | 只返回当前登录读者自己的借阅记录 |
| `帮我归还《数据库系统概论》` | 根据书名归还当前读者自己的未归还记录 |
| `第一本`、`就这本` | 能基于上一轮候选完成确认 |
| `算了，我不借了` | 取消待确认借阅操作 |

Agent 写操作审计检查：

```sql
SELECT
    log_id,
    user_id,
    action_type,
    target_book_id,
    target_borrow_id,
    tool_name,
    result_success,
    result_message,
    create_time
FROM agent_operation_log
ORDER BY create_time DESC
LIMIT 10;
```

预期：

```text
Agent 借书、还书等写操作能够写入 agent_operation_log，便于追踪和审计。
```

## 8. 展示稳定性检查

展示前建议按以下顺序最终确认：

1. MySQL 8.0 已启动。
2. `library` 数据库存在，且包含 `agent_operation_log` 表。
3. 后端启动成功，端口为 `8081`。
4. 前端启动成功，可以访问 Vite 页面。
5. 三类演示账号均可登录。
6. 至少有一本数据库相关图书处于可借状态。
7. Agent 演示前已设置 `DEEPSEEK_API_KEY`。
8. 演示前不要重复借同一本图书，避免图书状态已变成借出中。

## 9. 已知注意事项

| 问题 | 说明 |
| --- | --- |
| 自增 ID 跳号 | MySQL 自增主键只保证唯一递增，不保证连续，删除、回滚或插入失败都可能导致跳号 |
| Agent 依赖外部 API | 未设置 `DEEPSEEK_API_KEY` 时，Agent 会退化为本地兜底逻辑 |
| 图书不做物理删除 | 有借阅历史的图书通过下架处理，避免破坏历史记录 |
| 普通模糊查询未做全文索引 | `LIKE '%关键词%'` 对普通 B+Tree 索引利用有限，后续可考虑全文索引 |

## 10. 测试结论

本项目最终版本已完成数据库、后端和前端的基本联动，能够支持用户登录、图书管理、用户管理、借阅办理、归还办理、借阅记录查询、统计查询和读者 Agent 借书助手等核心功能。

数据库层面已完成主键、外键、唯一约束、非空约束、默认值约束、检查约束和必要索引设计；业务层面通过状态更新和外键约束保证借阅数据一致性；Agent 写操作通过审计日志进行追踪。

