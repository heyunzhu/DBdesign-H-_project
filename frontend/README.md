# Library Frontend

Vue3 + Vite frontend for the library borrowing management system.

## Run

Make sure the backend is running on `http://localhost:8081`.

```bash
npm.cmd install
npm.cmd run dev
```

Then open:

```text
http://127.0.0.1:5173
```
